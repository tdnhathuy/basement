import * as Components from './components';
import { useStyle } from './hooks';
import {
  isIOS,
  formatDate,
  formatNumber,
  genId,
  genShadow,
  isIphoneWithNotch,
  removeUnicode,
  parseArrToOptions,
} from './helpers';

export {
  Components,
  useStyle,
  formatDate,
  formatNumber,
  genId,
  genShadow,
  isIphoneWithNotch,
  removeUnicode,
  parseArrToOptions,
  isIOS,
};
